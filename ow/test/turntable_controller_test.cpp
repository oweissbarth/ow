#include "ow/interactive/camera_controller.h"
#include <catch2/catch_approx.hpp>
#include <catch2/catch_test_macros.hpp>

using namespace ow;

// Test for the turntable camera controller
TEST_CASE("Turntable camera controller"){
   /*interaction::TurntableCameraController controller;
    controller.setMousePosition(ow::math::Vec2f(0.0, 0.0));
    controller.setMousePosition(ow::math::Vec2f(0.0, 0.0));

    controller.update();

    REQUIRE(Catch::Approx(controller.view().pos.x).margin(1e-6) == 0.0);
    REQUIRE(Catch::Approx(controller.view().pos.y).margin(1e-6) == 0.0);
    REQUIRE(Catch::Approx(controller.view().pos.z).margin(1e-6) == 0.0);

    REQUIRE(Catch::Approx(controller.view().orientation.w).margin(1e-6) == 1.0);
    REQUIRE(Catch::Approx(controller.view().orientation.x).margin(1e-6) == 0.0);
    REQUIRE(Catch::Approx(controller.view().orientation.y).margin(1e-6) == 0.0);
    REQUIRE(Catch::Approx(controller.view().orientation.z).margin(1e-6) == 0.0);


    interaction::TurntableCameraController controller2;

    controller2.setMousePosition(ow::math::Vec2f(250.0, 0.0));
    controller2.setMousePosition(ow::math::Vec2f(250.0, 0.0));

    controller2.update();

    REQUIRE(Catch::Approx(controller2.view().pos.x).margin(1e-6) == 0.0);
    REQUIRE(Catch::Approx(controller2.view().pos.y).margin(1e-6) == 0.0);
    REQUIRE(Catch::Approx(controller2.view().pos.z).margin(1e-6) == 0.0);

    REQUIRE(Catch::Approx(controller2.view().orientation.w).margin(1e-6) == 1.0);
    REQUIRE(Catch::Approx(controller2.view().orientation.x).margin(1e-6) == 0.0);
    REQUIRE(Catch::Approx(controller2.view().orientation.y).margin(1e-6) == 0.0);
    REQUIRE(Catch::Approx(controller2.view().orientation.z).margin(1e-6) == 0.0);*/

}